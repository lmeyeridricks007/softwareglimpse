# FR-002 factory pack wave — factory-pack-2026-09-10-w9

Goal: fewer template-like pages, more genuinely valuable buying content.
URLs preserved. No bulk promote.

## Wave metrics

| Metric | Count |
| --- | ---: |
| Processed (legitimate existence) | 22 |
| Materially improved | 2 |
| Promoted | 2 |
| Remaining improve (this wave) | 20 |
| Semantic failure (still blocks) | 20 |
| Semantic risk reduced | 2 |
| Still high semantic risk | 20 |
| Average semantic similarity Δ (↓ better) | 0.026 |
| Unique-analysis signals gained | 24 |
| Data blockers | 0 |
| Evidence blockers | 7 |
| Average quality delta | 0.36 |
| Factory IMPROVE remaining (approx) | 499 |
| Already processed (excluded) | 352 |

## Notes

- Pages failing “why exist separately?” stay IMPROVE without rewrite.
- Semantic uniqueness requires distinct thesis/fit/limitations — not name/price swaps.
- Lane A empty for factory IMPROVE (no GSC demand in opportunity export); Lane B commercial packs prioritized.

## Pages

- `siteground-setup` [C/setup] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Configure the product for a first working workflow
- `smartproxy-implementation` [C/implementation] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Execute a product-specific implementation sequence with pitfalls
- `smartproxy-migration` [C/migration] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Plan cutover from a prior stack with product-specific risks
- `smartproxy-setup` [C/setup] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Configure the product for a first working workflow
- `splunk-implementation` [C/implementation] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Execute a product-specific implementation sequence with pitfalls
- `splunk-migration` [C/migration] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Plan cutover from a prior stack with product-specific risks
- `splunk-setup` [C/setup] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Configure the product for a first working workflow
- `squadcast-implementation` [C/implementation] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Execute a product-specific implementation sequence with pitfalls
- `squadcast-migration` [C/migration] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Plan cutover from a prior stack with product-specific risks
- `squadcast-setup` [C/setup] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Configure the product for a first working workflow
- `sysaid-implementation` [C/implementation] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Execute a product-specific implementation sequence with pitfalls
- `sysaid-migration` [C/migration] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Plan cutover from a prior stack with product-specific risks
- `sysaid-setup` [C/setup] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Configure the product for a first working workflow
- `topdesk-implementation` [C/implementation] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Execute a product-specific implementation sequence with pitfalls
- `topdesk-migration` [C/migration] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Plan cutover from a prior stack with product-specific risks
- `topdesk-setup` [C/setup] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Configure the product for a first working workflow
- `wp-engine-implementation` [C/implementation] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Execute a product-specific implementation sequence with pitfalls
- `wp-engine-migration` [C/migration] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Plan cutover from a prior stack with product-specific risks
- `wp-engine-setup` [C/setup] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Configure the product for a first working workflow
- `zyte-implementation` [C/implementation] applied=true Δq=4 risk high→elevated (sim 1.00→0.71) uniq+13 promoted=true — Execute a product-specific implementation sequence with pitfalls
- `zyte-migration` [C/migration] applied=false Δq=0 risk high→high (sim 1.00→1.00) uniq+0 promoted=false — Plan cutover from a prior stack with product-specific risks
- `zyte-setup` [C/setup] applied=true Δq=4 risk high→elevated (sim 1.00→0.72) uniq+11 promoted=true — Configure the product for a first working workflow
