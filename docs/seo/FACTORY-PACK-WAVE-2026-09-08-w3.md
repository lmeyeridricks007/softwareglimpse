# FR-002 factory pack wave — factory-pack-2026-09-08-w3

Goal: fewer template-like pages, more genuinely valuable buying content.
URLs preserved. No bulk promote.

## Wave metrics

| Metric | Count |
| --- | ---: |
| Processed (legitimate existence) | 50 |
| Materially improved | 25 |
| Promoted | 0 |
| Remaining improve (this wave) | 50 |
| Semantic failure | 16 |
| Data blockers | 0 |
| Evidence blockers | 0 |
| Average quality delta | 0.12 |
| Factory IMPROVE remaining (approx) | 1056 |
| Already processed (excluded) | 100 |

## Notes

- Pages failing “why exist separately?” stay IMPROVE without rewrite.
- Semantic uniqueness requires distinct thesis/fit/limitations — not name/price swaps.
- Lane A empty for factory IMPROVE (no GSC demand in opportunity export); Lane B commercial plans prioritized.

## Pages

- `wrike-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `accelerated-growth-studio-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `accelerated-growth-studio-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `accelerated-growth-studio-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `evolve-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `evolve-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `lucrovox-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `lucrovox-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `evolve-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `lucrovox-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `medusa-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `saleor-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `bigcommerce-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `commercetools-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `ecwid-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `lightspeed-retail-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `magento-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `opencart-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `prestashop-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `salesforce-commerce-cloud-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `shopware-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `square-online-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `squarespace-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `tiendanube-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `vtex-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `webflow-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `wix-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `woocommerce-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `bolt-for-business-implementation` [B/implementation] applied=true Δq=2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `bolt-for-business-migration` [B/migration] applied=true Δq=2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `bolt-for-business-setup` [B/setup] applied=true Δq=4 semanticFail=false promoted=false — Configure the product for a first working workflow
- `carepatron-implementation` [B/implementation] applied=true Δq=2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `carepatron-migration` [B/migration] applied=true Δq=2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `carepatron-setup` [B/setup] applied=true Δq=4 semanticFail=false promoted=false — Configure the product for a first working workflow
- `rippling-implementation` [B/implementation] applied=true Δq=2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `rippling-migration` [B/migration] applied=true Δq=2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `rippling-setup` [B/setup] applied=true Δq=4 semanticFail=false promoted=false — Configure the product for a first working workflow
- `dialpad-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `eightx8-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `five9-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `genesys-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `goto-connect-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `grasshopper-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `manychat-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `microsoft-teams-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `nextiva-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `ooma-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `openphone-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `respond-io-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `ringcentral-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
