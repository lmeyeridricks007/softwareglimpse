# FR-002 factory pack wave — factory-pack-2026-09-08-w2

Goal: fewer template-like pages, more genuinely valuable buying content.
URLs preserved. No bulk promote.

## Wave metrics

| Metric | Count |
| --- | ---: |
| Processed (legitimate existence) | 50 |
| Materially improved | 19 |
| Promoted | 0 |
| Remaining improve (this wave) | 50 |
| Semantic failure | 10 |
| Data blockers | 0 |
| Evidence blockers | 0 |
| Average quality delta | 0.6 |
| Factory IMPROVE remaining (approx) | 1056 |
| Already processed (excluded) | 50 |

## Notes

- Pages failing “why exist separately?” stay IMPROVE without rewrite.
- Semantic uniqueness requires distinct thesis/fit/limitations — not name/price swaps.
- Lane A empty for factory IMPROVE (no GSC demand in opportunity export); Lane B commercial plans prioritized.

## Pages

- `bamboohr-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `hibob-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `lever-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `oracle-hcm-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `when-i-work-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `7shifts-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `ashby-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `dayforce-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `deputy-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `greenhouse-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `gusto-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `homebase-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `paycor-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `paylocity-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `personio-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `ukg-pro-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `workable-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `workday-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `activecampaign-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `activecampaign-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `aweber-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `aweber-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `bouncer-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `bouncer-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `campaign-monitor-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `campaign-monitor-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `getresponse-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `getresponse-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `inboxally-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `inboxally-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `kit-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `kit-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `activecampaign-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `aweber-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `bouncer-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `campaign-monitor-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `getresponse-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `inboxally-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `kit-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `airtable-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `asana-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `basecamp-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `jira-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `linear-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `microsoft-project-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `motion-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `notion-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `smartsheet-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `todoist-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `trello-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
