# FR-002 factory pack wave — 2026-09-08

Goal: fewer template-like pages, more genuinely valuable buying content.
URLs preserved. No bulk promote.

## Wave metrics

| Metric | Count |
| --- | ---: |
| Processed (legitimate existence) | 50 |
| Materially improved | 23 |
| Promoted | 0 |
| Remaining improve (this wave) | 50 |
| Semantic failure | 6 |
| Data blockers | 0 |
| Evidence blockers | 0 |
| Average quality delta | -0.6 |
| Factory IMPROVE remaining (approx) | 1056 |

## Notes

- Pages failing “why exist separately?” stay IMPROVE without rewrite.
- Semantic uniqueness requires distinct thesis/fit/limitations — not name/price swaps.
- Lane A empty for factory IMPROVE (GSC opportunity export has almost no factory URLs); wave used Lane B commercial `*-plans` + sales-intel workflow packs.
- Family QA flagged shared patterns — correctly blocked promotion.
- 21 pages had quality Δ −2 after apply (implementation/setup/migration cluster); left IMPROVE.
- Worth-it pages often fail existence gate (`missing_limitations_or_poor_fit`) — stay IMPROVE until catalogue fit data exists.

## Pages

- `accelerated-growth-studio-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `evolve-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `lucrovox-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `rippling-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `bolt-for-business-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `carepatron-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `clickup-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `foxit-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `getscreen-me-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `hive-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `monday-plans` [B/plans] applied=false Δq=0 semanticFail=true promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `drip-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `flodesk-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `klaviyo-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `mailerlite-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `mailjet-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `moosend-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `omnisend-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `amplemarket-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `amplemarket-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `apollo-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `apollo-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `apollo-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `bookyourdata-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `bookyourdata-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `closely-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `closely-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `lusha-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `lusha-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `reply-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `reply-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `snov-migration` [B/migration] applied=true Δq=-2 semanticFail=false promoted=false — Plan cutover from a prior stack with product-specific risks
- `snov-setup` [B/setup] applied=true Δq=-2 semanticFail=false promoted=false — Configure the product for a first working workflow
- `amplemarket-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `bookyourdata-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `closely-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `lusha-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `reply-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `snov-implementation` [B/implementation] applied=true Δq=-2 semanticFail=false promoted=false — Execute a product-specific implementation sequence with pitfalls
- `agorapulse-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `brandwatch-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `braze-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `clickfunnels-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `iterable-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `later-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `marketo-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `meltwater-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `sprout-social-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `uniqode-plans` [B/plans] applied=true Δq=0 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
- `adp-workforce-now-plans` [B/plans] applied=true Δq=4 semanticFail=false promoted=false — Compare plan economics and thresholds where a higher tier becomes worthwhile
