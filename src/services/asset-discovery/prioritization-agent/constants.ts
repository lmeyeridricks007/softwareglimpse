/** Flagship CRM reviews — higher buyer / commercial leverage. */
export const FLAGSHIP_PRODUCT_SLUGS = new Set([
  "hubspot",
  "salesforce",
  "pipedrive",
  "zoho-crm",
  "freshsales",
  "dynamics-365",
]);

/** High-leverage hub sections for software pages. */
export const HIGH_IMPACT_SOFTWARE_SECTIONS = new Set([
  "overview",
  "features",
  "implementation",
  "use-cases",
  "screenshots",
]);

/** Minimum pattern size before collapsing to TEMPLATE FIX. */
export const SYSTEMIC_MIN_COUNT = 5;
