/**
 * @deprecated Import from `@/seo/canonical` for new code.
 * Re-exports preserved for existing `@/lib/urls` callers.
 */
export {
  absoluteUrl,
  canonicalUrl,
  normalizePath,
  resolveCanonicalPath,
} from "@/seo/canonical";
