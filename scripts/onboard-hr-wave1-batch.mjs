#!/usr/bin/env node
/**
 * HR Wave-1 affiliate products:
 * breezy-hr, connecteam, jibble, trainual.
 *
 * LearnWorlds is already marketing-primary — NOT rewritten here.
 * Secondary HR wiring is handled by patch-software-seed-hr-wave1.mjs.
 *
 * Usage: node scripts/onboard-hr-wave1-batch.mjs
 * Idempotent — overwrites product research / editorial packs for the four primaries.
 *
 * Does NOT auto-publish beyond writing approved editorial JSON.
 * Affiliate economics never enter scores.
 * No WordPress auto-publish.
 *
 * Methodology: hr-editorial v1.0.0, handsOnTesting=false.
 */
import {
  configureHrRuntime,
  runHrBatch,
  weightedScore,
  ROOT,
} from "./lib/hr-onboard-runtime.mjs";
import {
  PRODUCTS,
  COMPARISON_PAIRS,
} from "./lib/hr-wave1-products.mjs";

configureHrRuntime({
  verifiedAt: "2026-08-17T20:00:00.000Z",
  publishedAt: "2026-08-17T00:00:00.000Z",
  batchLabel: "HR Wave-1",
  jobTag: "hr-wave1",
  relatedGuidePaths: [
    "/categories/hr/",
    "/best/hr-software/",
  ],
});

function main() {
  console.log("HR Wave-1 products:");
  for (const p of PRODUCTS) {
    console.log(
      `  - ${p.slug} overall=${weightedScore(p.scores)} role=${p.membershipRole} cluster=${p.jobCluster}`,
    );
  }
  runHrBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_hr-wave1-seed-snippet.ts",
    comparisonSpecFile: "_hr-wave1-comparisons.json",
    videoSpecFile: "_hr-wave1-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-hr-wave1-batch.mjs
// Append into src/data/seed/software.ts before the closing ]; of softwareSeed
// via: node scripts/patch-software-seed-hr-wave1.mjs
// HR Wave-1 primaries: breezy-hr, connecteam, jibble, trainual.
// LearnWorlds: marketing-primary — patch secondaryCategorySlugs only (do not overwrite).`,
  });

  console.log("\nNext:");
  console.log("  1. node scripts/patch-software-seed-hr-wave1.mjs");
  console.log("  2. Wire affiliates via npm run affiliate:set if programmes missing");
  console.log("  3. Comparisons: Wave-1 clusters are thin — prefer 0 forced pairs (documented in report)");
  console.log("  4. node scripts/fetch-brand-logos.mjs breezy-hr connecteam jibble trainual");
  console.log("  5. Content quality audit — product-review CQ target ≥75 (aim 91)");
  console.log("  6. No WordPress auto-publish");
  console.log(`\nRoot: ${ROOT}`);
}

main();
