#!/usr/bin/env node
/**
 * Business Communications Priority-1 credibility products (non-affiliate):
 * RingCentral, Dialpad, Zoom, Nextiva, Microsoft Teams, Slack.
 *
 * Usage: node scripts/onboard-bc-priority1-batch.mjs
 * Idempotent — overwrites product research / editorial packs.
 *
 * Does NOT auto-publish beyond writing approved editorial JSON.
 * Affiliate economics never enter scores.
 * No WordPress auto-publish.
 *
 * Methodology: business-communications-editorial v1.0.0, handsOnTesting=false.
 *
 * Pricing grounding (retrieved / cross-checked 2026-08-17):
 * - RingCentral RingEX: Core/Advanced/Ultra ~$20/$25/$35 per user/mo annual
 *   (medium confidence — seat dollars often behind selector/quote).
 * - Dialpad Connect: Standard $15 / Pro $25 per user/mo annual (medium —
 *   pricing page is product-tab heavy).
 * - Zoom Phone / Workplace: Phone Unlimited ~$15–16; Pro Plus / Business Plus
 *   phone bundles ~$20.50 / $24.50 annual (medium — regional variance).
 * - Nextiva: Core $15 / Engage $25 / Scale $75 per user/mo annual (high —
 *   nextiva.com/nextiva-pricing).
 * - Microsoft Teams Phone: Standard $10; PAYG $13; Calling Plan $17;
 *   Domestic+Intl $34 per user/mo annual — requires separate Teams/M365 license.
 * - Slack: Free $0; Pro $7.25 / Business+ $15 per user/mo annual (high —
 *   slack.com/pricing).
 */
import {
  configureBcRuntime,
  runBcBatch,
  ROOT,
} from "./lib/bc-onboard-runtime.mjs";
import {
  PRODUCTS,
  COMPARISON_PAIRS,
} from "./lib/bc-priority1-products.mjs";

configureBcRuntime({
  verifiedAt: "2026-08-17T12:00:00.000Z",
  publishedAt: "2026-08-17T00:00:00.000Z",
  batchLabel: "Business-communications Priority-1",
  jobTag: "business-communications-priority1",
  relatedGuidePaths: [
    "/categories/business-communications/",
    "/best/business-communications-software/",
  ],
});

function main() {
  runBcBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_bc-priority1-seed-snippet.ts",
    comparisonSpecFile: "_bc-priority1-comparisons.json",
    videoSpecFile: "_bc-priority1-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-bc-priority1-batch.mjs
// Append into src/data/seed/software.ts before the closing ]; of softwareSeed
// via: node scripts/patch-software-seed-bc-priority1.mjs
// BC Priority-1 (non-affiliate): ringcentral, dialpad, zoom, nextiva,
// microsoft-teams, slack.`,
  });

  console.log("\nNext:");
  console.log("  1. node scripts/patch-software-seed-bc-priority1.mjs");
  console.log("  2. Update category seedProductSlugs + best.ts phone ranks / landscape");
  console.log("  3. Add approvedBcPair() entries in comparisons.ts");
  console.log("  4. Generate SG lettermark logos under public/brands/{slug}.png");
  console.log("  5. Generate teaching visuals public/software/{slug}/overview.png + workflow.png");
  console.log("  6. Content quality audit — product-review CQ target ≥75 (aim 91)");
  console.log("  7. No WordPress auto-publish");
  console.log(`\nRoot: ${ROOT}`);
}

main();
