#!/usr/bin/env node
/**
 * Business Communications Priority-2 credibility products (non-affiliate):
 * OpenPhone, 8x8 (slug eightx8), GoTo Connect, Grasshopper, respond.io.
 *
 * Closes Batch B remainder + Batch C core from
 * docs/reports/business-communications-product-coverage.md.
 *
 * Usage: node scripts/onboard-bc-priority2-batch.mjs
 * Idempotent — overwrites product research / editorial packs.
 *
 * Does NOT auto-publish beyond writing approved editorial JSON.
 * Affiliate economics never enter scores.
 * No WordPress auto-publish.
 *
 * Methodology: business-communications-editorial v1.0.0, handsOnTesting=false.
 *
 * Pricing grounding (retrieved / cross-checked 2026-08-17):
 * - OpenPhone / Quo: Starter/Business/Scale $15/$23/$35 per user/mo annual (high —
 *   openphone.com/pricing).
 * - 8x8 Work: X2 ~$24 / X4 ~$44 per user/mo annual (medium — first-party often
 *   bot/quote gated; Express ~$15; Contact Center ~$85–$140 cited).
 * - GoTo Connect: quote-only on goto.com/connect/pricing; industry ranges
 *   ~$26 / ~$34 / ~$80 low confidence — do not invent list dollars.
 * - Grasshopper: True Solo/Solo Plus/Small Business ~$14/$25/$55 mo annual
 *   flat (medium — interactive pricing page).
 * - respond.io: Starter/Growth/Advanced $79/$159/$279 mo yearly toggle (high —
 *   respond.io/pricing).
 */
import {
  configureBcRuntime,
  runBcBatch,
  ROOT,
} from "./lib/bc-onboard-runtime.mjs";
import {
  PRODUCTS,
  COMPARISON_PAIRS,
} from "./lib/bc-priority2-products.mjs";

configureBcRuntime({
  verifiedAt: "2026-08-17T18:00:00.000Z",
  publishedAt: "2026-08-17T00:00:00.000Z",
  batchLabel: "Business-communications Priority-2",
  jobTag: "business-communications-priority2",
  relatedGuidePaths: [
    "/categories/business-communications/",
    "/best/business-communications-software/",
  ],
});

function main() {
  runBcBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_bc-priority2-seed-snippet.ts",
    comparisonSpecFile: "_bc-priority2-comparisons.json",
    videoSpecFile: "_bc-priority2-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-bc-priority2-batch.mjs
// Append into src/data/seed/software.ts before the closing ]; of softwareSeed
// via: node scripts/patch-software-seed-bc-priority2.mjs
// BC Priority-2 (non-affiliate): openphone, eightx8, goto-connect,
// grasshopper, respond-io.`,
  });

  console.log("\nNext:");
  console.log("  1. node scripts/patch-software-seed-bc-priority2.mjs");
  console.log("  2. Update category seedProductSlugs + best.ts phone ranks / landscape");
  console.log("  3. Add approvedBcPair() entries in comparisons.ts");
  console.log("  4. Generate SG lettermark logos under public/brands/{slug}.png");
  console.log("  5. Generate teaching visuals public/software/{slug}/overview.png + workflow.png");
  console.log("  6. Content quality audit — product-review CQ target ≥75 (aim 91)");
  console.log("  7. No WordPress auto-publish");
  console.log(`\nRoot: ${ROOT}`);
}

main();
