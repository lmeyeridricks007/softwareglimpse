#!/usr/bin/env node
/**
 * Project Management Wave-1 affiliate products:
 * monday (Work Management), Hive, Office Timeline, Foxit, Getscreen.me, WebCatalog.
 *
 * Usage: node scripts/onboard-pm-wave1-batch.mjs
 * Idempotent — overwrites product research / editorial packs.
 *
 * Does NOT auto-publish beyond writing approved editorial JSON.
 * Affiliate economics never enter scores.
 * No WordPress auto-publish.
 *
 * Methodology: project-management-editorial v1.0.0, handsOnTesting=false.
 *
 * NOTE: monday-sales-crm is CRM — this batch onboards monday (Work Management only).
 */
import {
  configurePmRuntime,
  runPmBatch,
  weightedScore,
  ROOT,
} from "./lib/pm-onboard-runtime.mjs";
import {
  PRODUCTS,
  COMPARISON_PAIRS,
} from "./lib/pm-wave1-products.mjs";

configurePmRuntime({
  verifiedAt: "2026-08-17T18:00:00.000Z",
  publishedAt: "2026-08-17T00:00:00.000Z",
  batchLabel: "Project-management Wave-1",
  jobTag: "project-management-wave1",
  relatedGuidePaths: [
    "/categories/project-management/",
    "/best/project-management-software/",
  ],
});

function main() {
  console.log("Project Management Wave-1 products:");
  for (const p of PRODUCTS) {
    console.log(
      `  - ${p.slug} overall=${weightedScore(p.scores)} role=${p.membershipRole} cluster=${p.jobCluster}`,
    );
  }
  runPmBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_pm-wave1-seed-snippet.ts",
    comparisonSpecFile: "_pm-wave1-comparisons.json",
    videoSpecFile: "_pm-wave1-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-pm-wave1-batch.mjs
// Append into src/data/seed/software.ts before the closing ]; of softwareSeed
// via: node scripts/patch-software-seed-pm-wave1.mjs
// PM Wave-1: monday, hive, office-timeline, foxit, getscreen-me, webcatalog.
// Do NOT collide with monday-sales-crm (CRM).`,
  });

  console.log("\nNext:");
  console.log("  1. node scripts/patch-software-seed-pm-wave1.mjs");
  console.log("  2. Wire affiliates via npm run affiliate:set if programmes missing");
  console.log("  3. Add approvedPmPair() entries in comparisons.ts");
  console.log("  4. node scripts/generate-pm-wave1-lettermarks.mjs");
  console.log("  5. node scripts/generate-pm-wave1-teaching-visuals.mjs");
  console.log("  6. Content quality audit — product-review CQ target ≥75 (aim 91)");
  console.log("  7. No WordPress auto-publish");
  console.log(`\nRoot: ${ROOT}`);
}

main();
