#!/usr/bin/env node
/**
 * Project-management Priority-1
 * Usage: node scripts/onboard-pm-priority1-batch.mjs
 */
import {
  configurePmRuntime,
  runPmBatch,
  weightedScore,
  ROOT,
} from "./lib/pm-onboard-runtime.mjs";
import {
  PRODUCTS,
  COMPARISON_PAIRS,
} from "./lib/pm-priority1-products.mjs";

configurePmRuntime({
  verifiedAt: "2026-08-17T20:00:00.000Z",
  publishedAt: "2026-08-17T00:00:00.000Z",
  batchLabel: "Project-management Priority-1",
  jobTag: "project-management-priority1",
  relatedGuidePaths: [
    "/categories/project-management/",
    "/best/project-management-software/",
  ],
});

function main() {
  console.log("Project-management Priority-1 products:");
  for (const p of PRODUCTS) {
    console.log(
      `  - ${p.slug} overall=${weightedScore(p.scores)} role=${p.membershipRole} cluster=${p.jobCluster}`,
    );
  }
  runPmBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_pm-priority1-seed-snippet.ts",
    comparisonSpecFile: "_pm-priority1-comparisons.json",
    videoSpecFile: "_pm-priority1-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-pm-priority1-batch.mjs
// Append via patch script. Methodology project-management-editorial v1.0.0.
// Affiliate economics excluded. handsOnTesting=false.`,
  });
  console.log("\nNext: patch software seed, update best.ts + comparisons, lettermarks, visuals.");
  console.log(`Root: ${ROOT}`);
}

main();
