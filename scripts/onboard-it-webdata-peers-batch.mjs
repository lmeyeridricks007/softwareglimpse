#!/usr/bin/env node
/**
 * IT web-data peers of Bright Data: oxylabs, scraperapi, apify, thordata.
 *
 * Usage: node scripts/onboard-it-webdata-peers-batch.mjs
 * Methodology: it-development-editorial v1.0.0, handsOnTesting=false.
 * Does NOT auto-publish.
 */
import {
  configureItRuntime,
  runItBatch,
  weightedScore,
  ROOT,
} from "./lib/it-onboard-runtime.mjs";
import { PRODUCTS, COMPARISON_PAIRS } from "./lib/it-webdata-peers-products.mjs";

configureItRuntime({
  verifiedAt: "2026-08-18T16:00:00.000Z",
  publishedAt: "2026-08-18T00:00:00.000Z",
  batchLabel: "IT web-data peers",
  jobTag: "it-webdata-peers",
  relatedGuidePaths: [
    "/categories/it-development/",
    "/best/it-development-software/",
    "/use-cases/web-data-collection/",
  ],
});

function main() {
  console.log("IT web-data peers:");
  for (const p of PRODUCTS) {
    console.log(
      `  - ${p.slug} overall=${weightedScore(p.scores)} cluster=${p.jobCluster}`,
    );
  }
  runItBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_it-webdata-peers-seed-snippet.ts",
    comparisonSpecFile: "_it-webdata-peers-comparisons.json",
    videoSpecFile: "_it-webdata-peers-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-it-webdata-peers-batch.mjs
// Append via: node scripts/patch-software-seed-it-webdata-peers.mjs
// IT web-data peers: oxylabs, scraperapi, apify, thordata.`,
  });
  console.log(`\nRoot: ${ROOT}`);
}

main();
