#!/usr/bin/env node
/**
 * IT gap-fill PaaS + incident: railway, heroku, squadcast
 * (SolarWinds Incident Response).
 *
 * Usage: node scripts/onboard-it-gapfill-paas-incident-batch.mjs
 * Methodology: it-development-editorial v1.0.0, handsOnTesting=false.
 * Does NOT auto-publish.
 */
import {
  configureItRuntime,
  runItBatch,
  weightedScore,
  ROOT,
} from "./lib/it-onboard-runtime.mjs";
import {
  PRODUCTS,
  COMPARISON_PAIRS,
} from "./lib/it-gapfill-paas-incident-products.mjs";

configureItRuntime({
  verifiedAt: "2026-08-18T22:00:00.000Z",
  publishedAt: "2026-08-18T00:00:00.000Z",
  batchLabel: "IT gap-fill PaaS + incident",
  jobTag: "it-gapfill-paas-incident",
  relatedGuidePaths: [
    "/categories/it-development/",
    "/best/it-development-software/",
    "/use-cases/cloud-paas/",
    "/use-cases/incident-oncall/",
  ],
});

function main() {
  console.log("IT gap-fill PaaS + incident:");
  for (const p of PRODUCTS) {
    console.log(
      `  - ${p.slug} overall=${weightedScore(p.scores)} cluster=${p.jobCluster}`,
    );
  }
  runItBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_it-gapfill-paas-incident-seed-snippet.ts",
    comparisonSpecFile: "_it-gapfill-paas-incident-comparisons.json",
    videoSpecFile: "_it-gapfill-paas-incident-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-it-gapfill-paas-incident-batch.mjs
// Append via: node scripts/patch-software-seed-it-gapfill-paas-incident.mjs
// IT gap-fill: railway, heroku, squadcast (SolarWinds Incident Response).`,
  });
  console.log(`\nRoot: ${ROOT}`);
}

main();
