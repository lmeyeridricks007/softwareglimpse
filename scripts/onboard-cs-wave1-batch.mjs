#!/usr/bin/env node
/**
 * CS Wave-1 products:
 * freshdesk, zendesk-suite, help-scout, gorgias, tidio, freshchat, livechat, zoho-desk, freshservice.
 *
 * Intercom skipped (BC-primary). NiceJob skipped (reputation-adjacent).
 *
 * Usage: node scripts/onboard-cs-wave1-batch.mjs
 * Idempotent — overwrites product research / editorial packs for all nine primaries.
 * Tidio: overwrites existing CRM-era research and re-homes to customer-service.
 *
 * Methodology: customer-service-editorial v1.0.0, handsOnTesting=false.
 */
import {
  configureCsRuntime,
  runCsBatch,
  weightedScore,
  ROOT,
} from "./lib/cs-onboard-runtime.mjs";
import {
  PRODUCTS,
  COMPARISON_PAIRS,
} from "./lib/cs-wave1-products.mjs";

configureCsRuntime({
  verifiedAt: "2026-08-18T12:00:00.000Z",
  publishedAt: "2026-08-18T00:00:00.000Z",
  batchLabel: "CS Wave-1",
  jobTag: "cs-wave1",
  relatedGuidePaths: [
    "/categories/customer-service/",
    "/best/customer-service-software/",
  ],
});

function main() {
  console.log("CS Wave-1 products:");
  for (const p of PRODUCTS) {
    console.log(
      `  - ${p.slug} overall=${weightedScore(p.scores)} role=${p.membershipRole} cluster=${p.jobCluster}`,
    );
  }
  runCsBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_cs-wave1-seed-snippet.ts",
    comparisonSpecFile: "_cs-wave1-comparisons.json",
    videoSpecFile: "_cs-wave1-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-cs-wave1-batch.mjs
// Append into src/data/seed/software.ts before the closing ]; of softwareSeed
// via: node scripts/patch-software-seed-cs-wave1.mjs
// CS Wave-1 primaries: freshdesk, zendesk-suite, help-scout, gorgias, tidio, freshchat, livechat, zoho-desk, freshservice.
// Tidio: re-homed from CRM-primary — patch replaces existing soft-tidio entry.`,
  });

  console.log("\nNext:");
  console.log("  1. node scripts/patch-software-seed-cs-wave1.mjs");
  console.log("  2. Wire affiliates via npm run affiliate:set if programmes missing");
  console.log("  3. node scripts/fetch-brand-logos.mjs freshdesk zendesk-suite help-scout gorgias freshchat livechat zoho-desk freshservice");
  console.log("  4. Content quality audit — product-review CQ target ≥75");
  console.log(`\nRoot: ${ROOT}`);
}

main();
