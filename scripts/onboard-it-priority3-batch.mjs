#!/usr/bin/env node
/**
 * IT Priority-3 credibility products: dynatrace, azure-devops.
 *
 * Usage: node scripts/onboard-it-priority3-batch.mjs
 * Methodology: it-development-editorial v1.0.0, handsOnTesting=false.
 * Does NOT auto-publish.
 */
import {
  configureItRuntime,
  runItBatch,
  weightedScore,
  ROOT,
} from "./lib/it-onboard-runtime.mjs";
import { PRODUCTS, COMPARISON_PAIRS } from "./lib/it-priority3-products.mjs";

configureItRuntime({
  verifiedAt: "2026-08-18T14:00:00.000Z",
  publishedAt: "2026-08-18T00:00:00.000Z",
  batchLabel: "IT Priority-3",
  jobTag: "it-priority3",
  relatedGuidePaths: [
    "/categories/it-development/",
    "/best/it-development-software/",
  ],
});

function main() {
  console.log("IT Priority-3 products:");
  for (const p of PRODUCTS) {
    console.log(
      `  - ${p.slug} overall=${weightedScore(p.scores)} cluster=${p.jobCluster}`,
    );
  }
  runItBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_it-priority3-seed-snippet.ts",
    comparisonSpecFile: "_it-priority3-comparisons.json",
    videoSpecFile: "_it-priority3-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-it-priority3-batch.mjs
// Append via: node scripts/patch-software-seed-it-priority3.mjs
// IT Priority-3: dynatrace, azure-devops.`,
  });
  console.log(`\nRoot: ${ROOT}`);
}

main();
