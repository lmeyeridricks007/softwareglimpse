#!/usr/bin/env node
/**
 * IT Priority-2 credibility products:
 * servicenow, jira-service-management, new-relic, grafana-cloud, pagerduty,
 * gitlab, bitbucket, cpanel.
 *
 * Usage: node scripts/onboard-it-priority2-batch.mjs
 * Methodology: it-development-editorial v1.0.0, handsOnTesting=false.
 * Does NOT auto-publish.
 */
import {
  configureItRuntime,
  runItBatch,
  weightedScore,
  ROOT,
} from "./lib/it-onboard-runtime.mjs";
import { PRODUCTS, COMPARISON_PAIRS } from "./lib/it-priority2-products.mjs";

configureItRuntime({
  verifiedAt: "2026-08-18T12:00:00.000Z",
  publishedAt: "2026-08-18T00:00:00.000Z",
  batchLabel: "IT Priority-2",
  jobTag: "it-priority2",
  relatedGuidePaths: [
    "/categories/it-development/",
    "/best/it-development-software/",
  ],
});

function main() {
  console.log("IT Priority-2 products:");
  for (const p of PRODUCTS) {
    console.log(
      `  - ${p.slug} overall=${weightedScore(p.scores)} cluster=${p.jobCluster}`,
    );
  }
  runItBatch({
    products: PRODUCTS,
    pairs: COMPARISON_PAIRS,
    seedSnippetFile: "_it-priority2-seed-snippet.ts",
    comparisonSpecFile: "_it-priority2-comparisons.json",
    videoSpecFile: "_it-priority2-official-videos.json",
    seedHeader: `// Auto-generated by scripts/onboard-it-priority2-batch.mjs
// Append via: node scripts/patch-software-seed-it-priority2.mjs
// IT Priority-2: servicenow, jira-service-management, new-relic, grafana-cloud, pagerduty, gitlab, bitbucket, cpanel.`,
  });
  console.log(`\nRoot: ${ROOT}`);
}

main();
